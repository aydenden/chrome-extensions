# 설정

## 설정 구조

```typescript
interface StorageSchema {
  // ... 회사 데이터
  siteConfigs: SiteConfig[];     // 사이트 설정
  aiSettings: AISettings;        // AI 설정
}
```

## 1. 사이트 설정 (SiteConfig)

사용자가 지원 사이트를 추가/수정할 수 있는 설정.

### 스키마

```typescript
interface SiteConfig {
  id: string;                    // 고유 ID
  name: string;                  // 표시 이름
  urlPatterns: string[];         // URL 매칭 패턴
  dataType: 'company_info' | 'finance' | 'review';
  extractMethods: ('text' | 'graph' | 'pdf')[];
  guideText: string;             // 수집 가이드 문구
}
```

### 기본값

```typescript
const defaultSiteConfigs: SiteConfig[] = [
  {
    id: 'wanted',
    name: '원티드',
    urlPatterns: ['wanted.co.kr/company/*'],
    dataType: 'company_info',
    extractMethods: ['text', 'graph'],
    guideText: '원티드에서 회사 검색 → 회사 상세 페이지'
  },
  {
    id: 'innoforest',
    name: '혁신의숲',
    urlPatterns: ['innoforest.co.kr/company/*/finance'],
    dataType: 'finance',
    extractMethods: ['text', 'graph'],
    guideText: '혁신의숲에서 검색 → 조직/재무 탭'
  },
  {
    id: 'dart',
    name: 'DART',
    urlPatterns: ['dart.fss.or.kr/*'],
    dataType: 'finance',
    extractMethods: ['pdf'],
    guideText: 'DART에서 회사 검색 → 사업보고서 → PDF 다운'
  },
  {
    id: 'smes',
    name: '중기벤처확인',
    urlPatterns: ['smes.go.kr/venturein/*'],
    dataType: 'finance',
    extractMethods: ['text'],
    guideText: '중기벤처확인에서 검색 → 재무정보 탭'
  },
  {
    id: 'blind',
    name: '블라인드',
    urlPatterns: ['blind.com/company/*'],
    dataType: 'review',
    extractMethods: ['text'],
    guideText: '블라인드에서 회사 검색 → 리뷰 페이지'
  },
  {
    id: 'jobplanet',
    name: '잡플래닛',
    urlPatterns: ['jobplanet.co.kr/companies/*'],
    dataType: 'review',
    extractMethods: ['text'],
    guideText: '잡플래닛에서 회사 검색 → 리뷰 페이지'
  }
];
```

### 설정 UI

```
┌─────────────────────────────────────────────────┐
│  사이트 설정                                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ 원티드 ─────────────────────────────────┐  │
│  │ URL 패턴: wanted.co.kr/company/*         │  │
│  │ 데이터 타입: 기업 정보                    │  │
│  │ 추출 방식: 텍스트, 그래프                 │  │
│  │ [수정] [삭제]                             │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  ┌─ 혁신의숲 ───────────────────────────────┐  │
│  │ ...                                       │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  [+ 사이트 추가]                                │
│                                                 │
│  [기본값으로 초기화]                            │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 2. AI 설정 (AISettings)

AI 분석에 사용되는 프롬프트와 가중치 설정.

### 스키마

```typescript
interface AISettings {
  prompts: {
    companyNameExtract: string;
    financialAnalysis: string;
    reviewAnalysis: string;
    overallScore: string;
  };
  weights: {
    financial: number;           // 0-100
    review: number;              // 0-100 (financial + review = 100)
  };
}
```

### 기본값

```typescript
const defaultAISettings: AISettings = {
  prompts: {
    companyNameExtract: `Extract the company name from the following text.
Return ONLY the company name, nothing else.
Text: {text}`,

    financialAnalysis: `Analyze this company's financial data and provide:
1. Estimated runway in months
2. Financial risk level (high/medium/low)
3. Key risk factors

Financial Data: {financial_data}

Respond in JSON: { "runway_months": number, "runway_confidence": "high"|"medium"|"low", "runway_reasoning": "string", "risk_level": "high"|"medium"|"low", "risk_factors": ["string"] }`,

    reviewAnalysis: `Analyze these company reviews and provide:
1. Key positive themes
2. Key negative themes
3. Overall summary

Reviews: {reviews}

Respond in JSON: { "positive_themes": ["string"], "negative_themes": ["string"], "summary": "string" }`,

    overallScore: `Based on the analysis, provide an overall score from 1 to 5.
Financial: Runway {runway_months} months, Risk {risk_level}
Reviews: Positive {positive_themes}, Negative {negative_themes}
Weights: Financial {financial_weight}%, Reviews {review_weight}%

Respond: { "score": number, "reasoning": "string" }`
  },
  weights: {
    financial: 60,
    review: 40
  }
};
```

### 설정 UI

```
┌─────────────────────────────────────────────────┐
│  AI 설정 (고급)                                  │
├─────────────────────────────────────────────────┤
│                                                 │
│  ── 가중치 ──                                   │
│                                                 │
│  재무 분석: [====60%====|----40%----] 리뷰 분석 │
│                                                 │
│  ── 프롬프트 ──                                 │
│                                                 │
│  회사명 추출:                                    │
│  ┌───────────────────────────────────────────┐  │
│  │ Extract the company name from the         │  │
│  │ following text...                         │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  재무 분석:                                      │
│  ┌───────────────────────────────────────────┐  │
│  │ Analyze this company's financial data...  │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  리뷰 분석:                                      │
│  ┌───────────────────────────────────────────┐  │
│  │ Analyze these company reviews...          │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  종합 점수:                                      │
│  ┌───────────────────────────────────────────┐  │
│  │ Based on the analysis, provide...         │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  [저장]  [기본값으로 초기화]                     │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 프롬프트 변수

프롬프트에서 사용 가능한 변수:

| 변수 | 설명 | 사용 위치 |
|------|------|-----------|
| `{text}` | 추출된 텍스트 | 회사명 추출 |
| `{financial_data}` | 재무 데이터 (JSON) | 재무 분석 |
| `{reviews}` | 리뷰 텍스트 목록 | 리뷰 분석 |
| `{runway_months}` | 예상 Runway | 종합 점수 |
| `{risk_level}` | 리스크 레벨 | 종합 점수 |
| `{positive_themes}` | 긍정 키워드 | 종합 점수 |
| `{negative_themes}` | 부정 키워드 | 종합 점수 |
| `{financial_weight}` | 재무 가중치 | 종합 점수 |
| `{review_weight}` | 리뷰 가중치 | 종합 점수 |

## 설정 페이지 접근

```
팝업 → 설정 아이콘 (⚙️) → 설정 페이지 (새 탭)
```

## 데이터 관리

### 내보내기/가져오기

```
┌─────────────────────────────────────────────────┐
│  데이터 관리                                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  [📤 데이터 내보내기 (JSON)]                     │
│  [📥 데이터 가져오기]                            │
│                                                 │
│  ⚠️ 모든 데이터 삭제                             │
│  [🗑️ 초기화]                                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 내보내기 형식

```json
{
  "version": "1.0.0",
  "exportedAt": "2025-01-15T12:00:00Z",
  "companies": [...],
  "siteConfigs": [...],
  "aiSettings": {...}
}
```

## 향후 확장 가능한 설정

현재는 구현하지 않지만 나중에 추가 가능:

- 테마 (라이트/다크)
- 언어 설정
- 자동 분석 on/off
- 알림 설정
- 단축키 설정
